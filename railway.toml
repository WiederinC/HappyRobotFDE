[build]
dockerfilePath = "api/Dockerfile"

[deploy]
startCommand = "sh -c 'python -m api.seed && uvicorn api.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
