[build]
dockerfilePath = "Dockerfile.dashboard"

[deploy]
startCommand = "sh -c 'streamlit run dashboard/streamlit_app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true'"
healthcheckPath = "/_stcore/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
